#-------------openssl.cnf----------------
[ req ]
default_bits = 2048 # Size of keys
default_keyfile = key.pem # name of generated keys
default_md = sha256 # message digest algorithm
string_mask = nombstr # permitted characters
distinguished_name = req_distinguished_name

[ req_distinguished_name ]
# Variable name   Prompt string
0.organizationName = Organization Name (company)
organizationalUnitName = Organizational Unit Name (department, division)
emailAddress = Email Address
emailAddress_max = 40
localityName = Locality Name (city, district)
stateOrProvinceName = State or Province Name (full name)
countryName = Country Name (2 letter code)
countryName_min = 2
countryName_max = 2
commonName = Common Name (hostname, IP, or your name)
commonName_max = 64


#-------------------Edit this section------------------------------
# Commented out sections beginning with [PG] for use by genPlaygroundCert.py
# Edit with appropriate values, but leave commented out!

countryName_default     = US
stateOrProvinceName_default = Maryland
localityName_default        = Baltimore
0.organizationName_default  = Playground
organizationalUnitName_default  = # Insert Team Name Here

# [PG] Base = # Insert Semester Code Here
# [PG] TeamCode = # Insert Team Code Here
# [PG] CA = # Insert fully qualified path to CA certificate (blank for self-signed)
# [PG] CAkey = # Insert fully qualified path to CA key (blank for self-signed)

#--------------------DO NOT EDIT if using genPlaygroundCert.py-----
#commonName_default          = filled in by script#

